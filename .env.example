# Environment Variables for Sport Metadata Generator

# Admin Password for authentication (REQUIRED in production)
# Must be at least 8 characters, recommended: use a strong password
ADMIN_PASSWORD=your-secure-password-here

# JWT Secret for token signing (REQUIRED in production)
# Must be at least 32 characters, recommended: use a randomly generated string
# You can generate one using: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-jwt-secret-key-at-least-32-characters-long

# Node Environment
# Set to 'production' for production deployments
NODE_ENV=development

# Optional: Allow localhost authentication (only for development)
# Set to 'true' to enable auto-login on localhost
ALLOW_LOCALHOST_AUTH=false

# Server Port (optional, defaults to 3001)
PORT=3001
